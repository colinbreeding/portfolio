// Animated Background
window.addEventListener("DOMContentLoaded", () => {
  VANTA.HALO({
    el: "#vanta",
    mouseControls: true,
    touchControls: true,
    gyroControls: false,
    minHeight: 200.0,
    minWidth: 200.0,
    backgroundColor: 0x0,
    xOffset: 0.12,
  });
});
